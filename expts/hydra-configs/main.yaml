defaults:

  # Accelerators
  - accelerator: cpu

  # Downstream linear-probe monitor (task configs can disable with probe.enabled=false)
  - probe: admet_linear

  # Pre-training/fine-tuning
  - architecture: toymix
  - tasks: toymix
  - training: toymix

  # Benchmarking
  - model: gcn

  # Specializations
  - training/accelerator: ${training}_${accelerator}
  - training/model: ${training}_${model}

constants:
  seed: 0
  norm: layer_norm
  wandb:
    entity: eddy26
    save_dir: null
    project: graphium
