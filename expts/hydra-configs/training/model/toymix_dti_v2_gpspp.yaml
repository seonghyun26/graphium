# @package _global_

constants:
  name: toymix_dti_v2_gpspp
  seed: 42
  max_epochs: 100
  data_dir: ./data/graphium/neurips2023/small-dataset
  raise_train_error: true
  datacache_path: ../datacache/toymix-dti-v2/

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/toymix-dti-v2/gpspp/${now:%Y-%m-%d_%H-%M-%S}/
