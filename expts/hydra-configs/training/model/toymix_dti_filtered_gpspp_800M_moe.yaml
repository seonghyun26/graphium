# @package _global_

# GPS++ 800M + MoE with the ToyMix + DTI 100K filtered dataset.
constants:
  name: toymix_dti_filtered_gpspp_800M_moe
  seed: 42
  max_epochs: 100
  data_dir: /home/shpark/prj-molrepr/graphium/data/graphium/neurips2023/small-dataset
  raise_train_error: true
  datacache_path: /home/shpark/prj-molrepr/datacache/toymix-dti-filtered/

datamodule:
  args:
    batch_size_training: 64
    num_workers: 0  # avoid CoW memory duplication with ram loading

trainer:
  model_checkpoint:
    dirpath: /home/shpark/prj-molrepr/graphium/models_checkpoints/toymix-dti-filtered/gpspp_800M_moe/${now:%Y-%m-%d_%H-%M-%S}/
