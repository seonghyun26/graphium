# @package _global_

constants:
  name: dti_pactivity_gcn
  seed: 42
  max_epochs: 100
  raise_train_error: true
  datacache_path: ../datacache/dti_pactivity/

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/dti_pactivity/gcn/${now:%Y-%m-%d_%H-%M-%S}/
