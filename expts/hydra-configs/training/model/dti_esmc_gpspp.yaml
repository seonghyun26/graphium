# @package _global_

constants:
  name: dti_esmc_gpspp
  seed: 42
  max_epochs: 100
  raise_train_error: true
  datacache_path: ../datacache/dti_esmc/

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/dti_esmc/gpspp/${now:%Y-%m-%d_%H-%M-%S}/
