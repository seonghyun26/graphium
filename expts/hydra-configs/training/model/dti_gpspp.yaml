# @package _global_

constants:
  name: dti_gpspp
  seed: 42
  max_epochs: 100
  raise_train_error: true
  datacache_path: ../datacache/dti/

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/dti/gpspp/${now:%Y-%m-%d_%H-%M-%S}/
