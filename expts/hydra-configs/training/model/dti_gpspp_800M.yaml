# @package _global_

# GPS++ 800M with the DTI dataset.
constants:
  name: dti_gpspp_800M
  seed: 42
  max_epochs: 100
  raise_train_error: true
  datacache_path: /home/shpark/prj-molrepr/datacache/dti/

datamodule:
  args:
    batch_size_training: 128
trainer:
  model_checkpoint:
    dirpath: /home/shpark/prj-molrepr/graphium/models_checkpoints/dti/gpspp_800M/${now:%Y-%m-%d_%H-%M-%S}/
