# @package _global_

# GPS++ 800M with the ToyMix dataset.
constants:
  name: toymix_gpspp_800M
  seed: 42
  max_epochs: 100
  data_dir: /home/shpark/prj-molrepr/graphium/data/graphium/neurips2023/small-dataset
  raise_train_error: true
  datacache_path: ./datacache/neurips2023-small/

datamodule:
  args:
    batch_size_training: 192

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/small-dataset/gpspp_800M/${now:%Y-%m-%d_%H-%M-%S}/
