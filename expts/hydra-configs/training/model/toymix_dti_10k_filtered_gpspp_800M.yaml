# @package _global_

# GPS++ 800M with the ToyMix + DTI 10K filtered dataset.
constants:
  name: toymix_dti_10k_filtered_gpspp_800M
  seed: 42
  max_epochs: 100
  data_dir: /home/shpark/prj-molrepr/graphium/data/graphium/neurips2023/small-dataset
  raise_train_error: true
  datacache_path: /home/shpark/prj-molrepr/datacache/toymix-dti-10k-filtered/

datamodule:
  args:
    batch_size_training: 64
    num_workers: 0  # avoid CoW memory duplication with ram loading

trainer:
  model_checkpoint:
    dirpath: /home/shpark/prj-molrepr/graphium/models_checkpoints/toymix-dti-10k-filtered/gpspp_800M/${now:%Y-%m-%d_%H-%M-%S}/
