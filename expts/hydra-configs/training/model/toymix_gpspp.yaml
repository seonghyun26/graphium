# @package _global_

# GPS++ model with the PCQMv2 dataset.
constants:
  name: toymix_gpspp_4layer
  seed: 42
  max_epochs: 100
  data_dir: expts/data/neurips2023/small-dataset
  raise_train_error: true
  datacache_path: ../datacache/neurips2023-small/

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/small-dataset/gpspp/${now:%Y-%m-%d_%H-%M-%S}/
