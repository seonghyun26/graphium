# @package _global_

# GPS++ deep model (32 layers, 512 dim) with the Largemix dataset.
constants:
  name: largemix_gpspp_deep_32layer
  seed: 42
  max_epochs: 100
  data_dir: ../data/neurips2023/large-dataset
  raise_train_error: true
  datacache_path: ../datacache/neurips2023-large/

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/large-dataset/gpspp_deep/${now:%Y-%m-%d_%H-%M-%S}/
