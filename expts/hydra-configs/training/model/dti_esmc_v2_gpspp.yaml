# @package _global_

constants:
  name: dti_esmc_v2_gpspp
  seed: 42
  max_epochs: 100
  raise_train_error: true
  datacache_path: ../datacache/dti_esmc_v2/

trainer:
  model_checkpoint:
    dirpath: models_checkpoints/dti_esmc_v2/gpspp/${now:%Y-%m-%d_%H-%M-%S}/
